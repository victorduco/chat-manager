# Design Community Mini App

Telegram Mini App для дизайн-комьюнити с интеграцией LangGraph.

## Возможности

### 💬 Сообщения (Интеграция с LangGraph)
- Показывает историю сообщений из LangGraph thread
- Автоматически определяет `chat_id` из Telegram WebApp
- Использует `chat_id` как `thread_id` в LangGraph
- Работает с группами и личными чатами

### 📋 Контент-секции
- **Вакансии** - актуальные вакансии для дизайнеров
- **Менторы** - профили менторов с рейтингами
- **Услуги** - предложения дизайн-услуг
- **Курсы** - образовательные программы
- **Ресурсы** - полезные материалы и инструменты

### 🎨 Дизайн
- Telegram-нативный стиль
- Автоматическая адаптация под тему пользователя
- Haptic feedback
- Sticky поиск и табы

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start
```

## Деплой на Heroku

```bash
# Создать приложение
heroku create your-app-name

# Задеплоить
git push heroku master

# Открыть в браузере
heroku open
```

## Интеграция с Telegram Bot

1. Откройте [@BotFather](https://t.me/botfather)
2. Выберите своего бота
3. Настройте Web App URL:
   ```
   /newapp
   Your Bot Name
   Design Community
   https://your-app-name.herokuapp.com
   https://your-app-name.herokuapp.com/icon.png (опционально)
   ```

4. Или добавьте кнопку в меню:
   ```
   /setmenubutton
   @your_bot
   Design Community
   https://your-app-name.herokuapp.com
   ```

## Интеграция с LangGraph

Миниапп автоматически:
1. Получает `chat_id` из Telegram WebApp initData
2. Использует его как `thread_id` для запросов к LangGraph
3. Показывает историю сообщений из треда
4. Поддерживает и групповые, и личные чаты

### Переменные окружения

Для dev-режима можно переопределить API URL:
```bash
# .env.local
VITE_LANGGRAPH_API_URL=http://localhost:2024
```

Production URL определяется автоматически в `src/services/api.js`.

## Архитектура

```
miniapp/
├── src/
│   ├── components/       # Vue компоненты
│   │   ├── SearchBar.vue
│   │   ├── TabBar.vue
│   │   ├── MessageCard.vue    # Для сообщений из LangGraph
│   │   ├── VacancyCard.vue
│   │   ├── MentorCard.vue
│   │   └── ...
│   ├── services/        # API сервисы
│   │   ├── api.js       # LangGraph API клиент
│   │   └── telegram.js  # Telegram WebApp утилиты
│   ├── data/
│   │   └── mockData.js  # Тестовые данные
│   ├── App.vue          # Главный компонент
│   └── main.js
├── server.js            # Express сервер для production
├── Procfile             # Heroku конфигурация
└── package.json
```

## API

Миниапп использует LangGraph Cloud API:

- `GET /threads/{thread_id}/state` - получить состояние треда
- `GET /threads/{thread_id}/history` - получить историю
- `POST /threads/{thread_id}/runs/wait` - создать run и дождаться ответа

Аутентификация через `X-Telegram-Init-Data` header (Telegram WebApp initData).

## Лицензия

GPL-3.0
